import { ClipLoader } from "react-spinners";

export default function Loading() {
  return (
    <div>
        <ClipLoader color="#64748b" className="relative left-[47%]" size={120} />
    </div>
);
}
